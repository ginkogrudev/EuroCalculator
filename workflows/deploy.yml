name: Piggy Calc LaunchPad
on:
  push:
    branches: [main]

jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Essential for both GH Pages and Releases
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v3
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - run: flutter pub get

      - name: Run Tests
        run: flutter test

      - name: Build Android APK
        run: flutter build apk --release

      - name: Build Web
        run: flutter build web --release --base-href "/EuroCalculator/"

      # 4. Deploy Web to gh-pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./build/web
          publish_branch: gh-pages

      # 5. NEW: Create GitHub Release and Upload APK
      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v1.0.${{ github.run_number }}
          name: Piggy Calc Release v1.0.${{ github.run_number }}
          files: build/app/outputs/flutter-apk/app-release.apk
          body: |
            ## Euro Piggy Update 🐷
            - Нова версия с опреснен дизайн и лого!
            - Подобрена поддръжка за споделяне на теми.
            - Тествано и готово за работа.
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      # 6. Keep Artifact as backup
      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: piggy-calc-android-apk
          path: build/app/outputs/flutter-apk/app-release.apk